---
- name: Update packages
  ansible.builtin.apt:
    upgrade: "safe"
    autoremove: true
    cache_valid_time: 3600

- name: Install essential packages
  ansible.builtin.apt:
    pkg:
      - git
      - htop
      - tmux
      - screen
      - 7zip
      - ncdu
      - stow
