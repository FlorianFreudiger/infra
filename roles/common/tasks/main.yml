---
- name: Update packages
  ansible.builtin.apt:
    upgrade: "safe"
    autoremove: true
    cache_valid_time: "{{ apt_cache_valid_time }}"

- name: Install essential packages
  ansible.builtin.apt:
    pkg:
      - git
      - htop
      - tmux
      - screen
      - ncdu
      - stow
